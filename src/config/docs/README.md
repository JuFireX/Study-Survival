# 配置模块文档

`src/config` 模块存储了游戏的所有静态数据配置，包括卡牌定义、关卡数值、题库和类型定义。

## 目录结构

- **`cards.ts`**: 定义所有卡牌数据（武器、Buff、题目卡）。
- **`evolution.ts`**: 定义世界等级和数值成长表。
- **`questions.ts`**: 题库数据源。
- **`types.ts`**: 核心 TypeScript 接口和类型定义。
- **`docs/`**: 文档文件。
