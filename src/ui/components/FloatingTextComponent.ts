import * as pc from 'playcanvas';

/**
 * 伤害跳字 UI 组件 (FloatingTextComponent)
 * 
 * 职责:
 * 1. 负责伤害跳字 DOM 元素的动态创建。
 * 2. 负责每帧更新文字在屏幕上的位置 (世界坐标 -> 屏幕坐标)。
 */
export class FloatingTextComponent extends pc.ScriptType {
    initialize() {
        // Initialize
    }

    update(dt: number) {
        // Update text positions
    }
}
