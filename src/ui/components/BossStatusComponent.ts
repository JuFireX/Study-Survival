import * as pc from 'playcanvas';

/**
 * Boss 状态 UI 组件 (BossStatusComponent)
 * 
 * 职责:
 * 1. 负责 Boss 血条 DOM 元素的创建和绑定。
 * 2. 将 DOM 事件转发给 BossStatus 逻辑类。
 */
export class BossStatusComponent extends pc.ScriptType {
    initialize() {
        // Create DOM elements
    }
}
