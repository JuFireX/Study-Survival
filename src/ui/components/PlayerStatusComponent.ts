import * as pc from 'playcanvas';

/**
 * 玩家状态 UI 组件 (PlayerStatusComponent)
 * 
 * 职责:
 * 1. 负责玩家血条和经验条 DOM 的渲染。
 * 2. 处理 CSS 样式变化以反映数值改变。
 */
export class PlayerStatusComponent extends pc.ScriptType {
    initialize() {
        // Initialize
    }
}
