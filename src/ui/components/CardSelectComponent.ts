import * as pc from 'playcanvas';

/**
 * 卡牌选择 UI 组件 (CardSelectComponent)
 * 
 * 职责:
 * 1. 负责选卡界面 DOM 元素的渲染。
 * 2. 监听 DOM 点击事件并通知 CardSystem。
 */
export class CardSelectComponent extends pc.ScriptType {
    initialize() {
        // Create DOM elements
    }
}
