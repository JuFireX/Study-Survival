import * as pc from 'playcanvas';

/**
 * 玩家特效 UI 组件 (PlayerEffectsComponent)
 * 
 * 职责:
 * 1. 负责状态栏图标区域的 DOM 管理。
 * 2. 动态添加/移除 Buff 图标 DOM。
 */
export class PlayerEffectsComponent extends pc.ScriptType {
    initialize() {
        // Initialize
    }
}
