/**
 * 玩家状态 UI 管理器 (PlayerStatus)
 * 
 * 职责:
 * 1. 显示玩家的生命值 (Health Bar)。
 * 2. 显示玩家的经验值 (Experience Bar) 和等级。
 * 3. 实时更新数值显示。
 */
export class PlayerStatus {
    constructor() {
        // Initialization
    }

    public updateHealth(current: number, max: number) {
        // Update HP
    }

    public updateExp(current: number, max: number) {
        // Update EXP
    }
}
